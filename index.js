export const throwDice = () => {
    return Math.floor(Math.random() * 6) + 1;
}

export const throwDices = (numDices) => {
    const dices = []; 
    
    return dices; 
}

export const beginYams = (numRounds) => {

}